# 若星空间（Starry Space）

# Agent 开发守则与架构规范

> 本文件是若星空间项目的工程宪法。
>
> 所有 AI Agent、Codex、Antigravity 以及人类开发者，在本仓库进行需求分析、编码、重构、数据库变更、接口开发、UI 实现、测试、修复和文档维护时，都必须遵守本规范。
>
> **核心原则：先理解，再修改；先验证，再完成；真实记录，不得臆测。**

---

# 1. 项目定位

## 1.1 产品定义

若星小程序是若星团队与学员之间的效能工具与陪伴工具。

以：

* 课程
* 活动
* 内容
* 会员
* 商城

为主要载体，帮助学员有序参与课程与活动，沉淀学习、实践与成长记录；同时帮助团队高效完成：

* 报名
* 收款
* 资料发布
* 内容管理
* 学员管理
* 学员陪伴

## 1.2 产品边界

本项目：

* 不是营销工具；
* 不做开放式 UGC 社区；
* 不做自有电商闭环交易；
* 商城商品原则上跳转第三方小程序或外部链接；
* 不制造焦虑；
* 注重温润陪伴；
* 注重极简、美观、克制的产品体验。

Agent 不得擅自突破产品边界增加功能。

---

# 2. Agent 工作总原则

Agent 必须遵循以下原则：

## 2.1 先理解，再修改

任何非简单任务开始前，必须先：

1. 阅读 AGENTS.md；
2. 阅读与任务相关的 PRD；
3. 阅读相关页面设计/原型；
4. 检查当前代码；
5. 检查相关数据模型和 API；
6. 理解现有实现；
7. 判断任务影响范围；
8. 制定实施方案。

不得在不了解现有代码的情况下直接大规模修改。

---

## 2.2 最小变更原则

只修改完成当前任务所必需的代码。

禁止：

* 无关重构；
* 无关文件格式化；
* 无关依赖升级；
* 无关数据库修改；
* 无关 UI 修改；
* 为了“顺便优化”而扩大任务范围。

如果发现其他问题：

记录为问题或后续任务，不要擅自扩大当前任务范围。

---

## 2.3 不确定时不得猜测

当发现以下情况时，不得自行做关键产品决策：

* PRD 与设计稿冲突；
* PRD 与现有代码冲突；
* 页面与 API 契约冲突；
* 数据模型与业务规则冲突；
* 技术方案存在重大安全风险；
* 需求存在歧义；
* 缺少必要业务规则；
* 不确定某个行为是否符合产品预期。

应：

1. 明确指出冲突；
2. 说明影响；
3. 给出可选方案；
4. 标记需要人工确认的事项。

低风险、局部、符合现有模式的技术实现可以自行判断。

---

# 3. 权威信息源与冲突处理

项目不同类型的信息具有不同的权威来源。

| 信息类型      | 权威来源                          |
| --------- | ----------------------------- |
| 产品需求      | `docs/PRD/`                   |
| 页面视觉与交互   | `docs/Design/`                |
| 当前系统真实状态  | 当前代码                          |
| 技术架构决策    | `docs/devlog/decisions.md`    |
| 当前开发状态    | `docs/devlog/progress.md`     |
| 历史开发过程    | `docs/devlog/daily/`          |
| 当前数据库结构   | `server/prisma/schema.prisma` |
| 当前 API 实现 | `server/src/`                 |
| 当前测试结果    | 实际测试结果及 `docs/testing/`       |

### 冲突处理原则

如果历史 DevLog 与当前代码冲突：

> 以当前代码和实际测试结果为准，并修正过时文档。

如果 PRD 与设计稿冲突：

> 不得自行决定产品行为，必须提出冲突。

如果技术文档与当前代码冲突：

> 先检查是否存在已记录的架构演进，再决定是否更新文档。

---

# 4. 项目目录与工程架构

项目采用 Monorepo。

```text
starry-mini2/

├── AGENTS.md
├── README.md
├── docker-compose.yml
├── .gitignore
│
├── docs/
│   ├── PRD/
│   ├── Design/
│   │   ├── VI/
│   │   └── prototype-v3/
│   │
│   ├── testing/
│   │   ├── README.md
│   │   ├── test-plan.md
│   │   ├── test-cases.md
│   │   ├── regression.md
│   │   └── test-report.md
│   │
│   └── devlog/
│       ├── README.md
│       ├── progress.md
│       ├── CHANGELOG.md
│       ├── architecture.md
│       ├── decisions.md
│       ├── issues.md
│       ├── lessons.md
│       └── daily/
│
├── server/
├── admin/
└── miniprogram/
```

---

# 5. 技术栈规范

## 5.1 Server

* Node.js 20 LTS
* Express
* TypeScript
* Prisma
* PostgreSQL 16

要求：

* 严格 TypeScript；
* 禁止无理由使用 `any`；
* 业务逻辑进入 Service；
* Controller 不承担复杂业务逻辑；
* 数据库访问通过 Prisma；
* 数据库结构变更必须通过 Prisma Migration。

---

## 5.2 Admin

* Vue 3
* Vite
* TypeScript
* TailwindCSS
* Pinia

要求：

* 页面对应 `prototype-v3/admin/`；
* 优先复用公共组件；
* API 类型保持同步；
* 不重复实现已有通用组件。

---

## 5.3 Miniprogram

* 微信小程序原生框架
* TypeScript
* npm
* WXSS

要求：

* 遵循 `docs/Design/VI/`；
* 遵循 `prototype-v3/`；
* 使用统一 Design Token；
* 优先组件化；
* 不在页面中重复实现通用逻辑。

---

# 6. 数据库规范

数据库：

> PostgreSQL 16

ORM：

> Prisma

数据库模型统一通过：

```text
server/prisma/schema.prisma
```

管理。

禁止：

* 手动修改生产数据库结构；
* 绕过 Prisma Migration；
* 未经确认删除字段；
* 未经确认修改数据类型；
* 未经确认破坏现有数据。

涉及数据库结构变化时必须：

1. 修改 Prisma Schema；
2. 创建 Migration；
3. 检查 Migration；
4. 执行相关测试；
5. 检查已有数据兼容性；
6. 更新相关 DevLog。

---

# 7. 用户体系

小程序用户与管理后台用户物理隔离。

## User

小程序学员/会员：

```text
id
openid
unionid
nickname
avatarUrl
phone
memberTier
memberExpireAt
points
status
createdAt
updatedAt
```

## AdminUser

管理后台成员：

```text
id
username
email
passwordHash
realName
avatarUrl
role
status
lastLoginAt
createdAt
updatedAt
```

不得将两套身份体系混合。

---

# 8. API 规范

## 8.1 路由

客户端：

```text
/api/v1/client/*
```

管理后台：

```text
/api/v1/admin/*
```

公共：

```text
/api/v1/public/*
```

微信支付回调：

```text
/api/v1/payments/wechat/notify
```

---

## 8.2 API 响应

统一响应结构：

```typescript
interface ApiResponse<T> {
  code: number;
  message: string;
  data?: T;
  timestamp: number;
}
```

成功：

```text
code = 0
```

失败：

```text
code != 0
```

不得让不同模块自行创造完全不同的响应格式。

---

# 9. 安全规范

敏感配置必须通过环境变量注入。

禁止硬编码：

* AppSecret
* JWT Secret
* 数据库密码
* 微信支付私钥
* API Key
* COS/OSS 密钥
* 其他生产凭证

敏感信息不得提交到 Git。

任何发现的密钥泄露必须立即报告，并视情况进行凭证轮换。

---

# 10. 文件与第三方服务

文件存储采用 Adapter 模式：

```typescript
interface StorageAdapter {
  uploadFile(
    file: Express.Multer.File,
    folder?: string
  ): Promise<{ url: string; key: string }>;

  deleteFile(key: string): Promise<boolean>;
}
```

允许：

```text
Local
COS
OSS
```

之间切换。

不得让业务代码直接依赖具体存储供应商。

---

# 11. 微信支付

微信支付 V3 必须遵循官方接口规范。

支付流程必须考虑：

* 统一下单；
* prepay_id；
* 客户端支付签名；
* 回调验签；
* AES-256-GCM 解密；
* 幂等处理；
* 订单状态一致性；
* 数据库事务。

订单状态变化必须保证幂等。

不得仅根据客户端结果认定支付成功。

最终支付状态必须以服务端可信结果为准。

---

# 12. UI / UX 规范

## 12.1 Admin

以：

```text
docs/Design/prototype-v3/admin/
```

为视觉与交互基准。

优先复用：

* StatCard
* DataTable
* FilterBar
* StatusBadge
* MediaUploader
* RichEditor

---

## 12.2 Miniprogram

以：

```text
docs/Design/design.md
.agents/rules/design-rules.md
docs/Design/VI/
docs/Design/prototype-v3/
```

为权威设计输入。

必须遵循统一：

* 色彩；
* 字体；
* 间距；
* 圆角；
* 阴影；
* 状态；
* 交互；
* 组件规范。

禁止为了方便开发而随意改变设计。

如果设计稿无法实现或与平台能力冲突，应明确说明原因。

---

# 13. 测试规范

> **测试与开发同等重要。**
>
> **代码完成不等于任务完成。**

任务完成必须满足：

```text
实现
+
验证
+
测试
+
修复
+
回归
+
验收
```

---

## 13.1 测试必须真实执行

不得仅通过：

* 阅读代码；
* 类型正确；
* 编译成功；
* 静态分析；
* “理论上可以运行”；

来声称功能完成。

能够实际执行的测试必须实际执行。

---

## 13.2 测试结果必须真实

测试状态必须区分：

```text
PASS       实际执行并通过
FAIL       实际执行但失败
NOT RUN    尚未执行
BLOCKED    因环境/依赖限制无法执行
```

禁止：

```text
NOT RUN → PASS
BLOCKED → PASS
理论正确 → PASS
```

不得伪造测试结果。

---

## 13.3 基础检查

根据项目实际配置执行：

* TypeScript 类型检查；
* ESLint；
* 单元测试；
* 构建；
* API 测试；
* 组件测试；
* 页面测试；
* 集成测试；
* E2E / 用户流程测试。

具体命令以各子项目 `package.json` 和项目测试文档为准。

不得凭空创造不存在的命令。

---

## 13.4 按修改范围测试

### 修改工具函数

必须测试：

* 正常输入；
* 异常输入；
* 边界输入。

### 修改组件

必须测试：

* 正常渲染；
* Props；
* 用户交互；
* Loading；
* Empty；
* Error；
* 边界状态。

### 修改页面

必须测试：

* 页面加载；
* 数据加载；
* 用户操作；
* 页面跳转；
* 返回；
* Loading；
* Empty；
* Error。

### 修改 API

必须测试：

* 正常请求；
* 参数错误；
* 未登录；
* 无权限；
* 数据不存在；
* 服务异常；
* 重复请求。

### 修改数据库

必须测试：

* Migration；
* CRUD；
* 数据兼容性；
* 相关业务流程；
* 回归测试。

---

# 14. 核心用户流程测试

对于核心业务，不允许只测试单个页面。

必须按照真实用户行为验证完整流程。

例如：

```text
打开小程序
↓
登录
↓
进入首页
↓
浏览课程
↓
进入课程详情
↓
报名
↓
支付
↓
进入学习
↓
完成学习
↓
打卡
↓
查看个人记录
```

每个核心流程必须能够追踪：

```text
入口
→ 操作
→ API
→ 数据变化
→ 页面反馈
→ 最终状态
```

---

# 15. 异常与边界测试

核心功能至少考虑：

## 数据状态

* Loading
* Empty
* Success
* Error

## 网络状态

* 网络失败；
* 超时；
* API 失败；
* 服务不可用。

## 用户状态

* 未登录；
* 已登录；
* 无权限；
* 权限不足；
* 用户被禁用。

## 操作状态

* 重复点击；
* 重复提交；
* 重复报名；
* 重复支付；
* 重复回调。

## 数据边界

* 空字符串；
* 空数组；
* 缺失字段；
* 最大长度；
* 最小长度；
* 极端数量；
* 非法参数。

---

# 16. 回归测试

任何修改公共模块或核心业务，都必须进行相关回归测试。

重点关注：

* 登录；
* 权限；
* 路由；
* API；
* 全局状态；
* 公共组件；
* 数据模型；
* 支付；
* 报名；
* 打卡；
* 用户中心。

不得因为“当前功能已经测试通过”就认为整个项目没有受到影响。

---

# 17. 测试失败处理

测试失败后必须：

```text
测试失败
↓
定位问题
↓
分析根因
↓
修复实现
↓
重新测试
↓
回归测试
↓
更新测试记录
```

禁止：

* 删除失败测试；
* 跳过测试；
* 降低断言；
* 修改测试以掩盖 Bug；
* 隐藏错误；
* 忽略错误输出。

如果暂时无法解决：

必须标记为：

```text
BLOCKED
```

并记录：

* 问题；
* 原因；
* 已尝试方案；
* 当前影响；
* 建议解决方案。

不得将阻塞问题伪装成完成。

---

# 18. Definition of Done

任何任务只有满足以下条件，才能标记为 Done：

* [ ] 功能已经实现；
* [ ] 符合 PRD；
* [ ] 符合设计稿；
* [ ] 类型检查通过；
* [ ] Lint 通过；
* [ ] 构建通过；
* [ ] 相关自动化测试通过；
* [ ] 核心用户流程验证通过；
* [ ] 必要的异常场景已经验证；
* [ ] 必要的回归测试已经完成；
* [ ] 没有已知高优先级问题；
* [ ] 测试结果真实记录；
* [ ] DevLog 已更新；
* [ ] 开发进度已更新。

如果某一项无法执行，必须明确标记：

```text
NOT RUN
```

或：

```text
BLOCKED
```

并说明原因。

---

# 19. 测试文档

详细测试策略和测试用例存放：

```text
docs/testing/
```

包括：

```text
README.md
test-plan.md
test-cases.md
regression.md
test-report.md
```

`AGENTS.md` 规定测试纪律。

`docs/testing/` 规定具体测试内容。

---

# 20. DevLog 工程记忆

`docs/devlog/` 是项目长期工程记忆库。

它记录：

> 系统为什么这样设计、如何演进、遇到过什么问题、做过什么重要决策。

---

# 21. 强制工作闭环

Agent 每次执行实际开发任务必须遵循：

```text
理解
↓
规划
↓
修改
↓
测试
↓
修复
↓
回归
↓
验收
↓
总结
↓
记录
↓
更新进度
```

禁止：

```text
理解
↓
修改
↓
测试
↓
结束
```

---

# 22. DevLog 更新规则

每个具有实际意义的任务完成后：

## 必须检查

* Git 变更；
* 新增文件；
* 修改文件；
* 删除文件；
* 数据库变化；
* API 变化；
* 测试变化。

## 判断是否产生长期知识

### P0：必须记录

* 架构变化；
* 数据库结构变化；
* Migration；
* API 契约变化；
* 核心技术决策；
* 重大 Bug；
* Breaking Change；
* 安全策略；
* 第三方服务集成。

### P1：建议记录

* 新功能；
* 重要模块；
* 性能优化；
* 开发规范变化；
* 高价值技术经验。

### P2：可选

* 普通 UI 微调；
* 小型重构；
* 常规 Bug 修复。

---

# 23. DevLog 真实性

严禁：

* 编造开发过程；
* 编造测试结果；
* 编造技术决策；
* 编造性能数据；
* 编造用户数据；
* 把未验证内容写成已验证；
* 把计划写成已完成。

必须区分：

```text
已完成
已验证
未验证
待确认
阻塞
计划
```

如果历史日志与当前代码冲突：

> 以当前代码和实际测试结果为准，并修正历史文档。

---

# 24. 技术决策记录

当产生重要技术决策时，应记录：

```text
docs/devlog/decisions.md
```

重点记录：

```text
背景
问题
候选方案
最终方案
为什么选择
放弃其他方案的原因
影响范围
未来可能变化
```

重点记录：

> Why，而不仅仅是 What。

---

# 25. Agent 任务执行规范

对于中大型任务：

1. 先分析；
2. 再制定计划；
3. 再执行；
4. 分阶段验证；
5. 不一次性进行不可控的大规模修改。

每个阶段完成后检查：

```text
代码
↓
测试
↓
回归
↓
文档
```

如果任务涉及多个模块，应尽量保持每次修改边界清晰。

---

# 26. Goal 模式规范

当使用 `/goal` 或类似持续执行模式时：

Agent 必须：

1. 读取当前项目状态；
2. 检查未完成任务；
3. 选择依赖已经满足的任务；
4. 执行；
5. 测试；
6. 修复；
7. 回归；
8. 更新 DevLog；
9. 更新进度；
10. 继续下一项任务。

不得因为：

* 单个功能完成；
* 单次测试通过；
* 代码量较大；
* 修改时间较长；

就自行宣布整个 Goal 完成。

只有当 Goal 的验收条件全部满足时，才能宣布 Goal 完成。

---

# 27. 人工决策边界

以下事项原则上需要人工确认：

* 产品需求变化；
* 核心业务规则变化；
* 数据库破坏性修改；
* 权限模型变化；
* 支付逻辑变化；
* 安全策略变化；
* 大规模架构变化；
* 删除核心功能；
* 修改 PRD；
* 修改设计原则；
* 引入重大第三方依赖；
* Breaking Change。

Agent 可以：

* 分析；
* 提供方案；
* 比较方案；
* 评估风险；

但不得擅自替代产品负责人做重大产品决策。

---

# 28. Git 与变更安全

开发前后应检查 Git 状态。

不得：

* 覆盖他人未提交修改；
* 删除无法确认归属的代码；
* 强制重置未知修改；
* 修改无关文件；
* 提交敏感信息。

涉及大量文件变化时，必须先检查变更范围。

---

# 29. 最终任务汇报

任务完成后必须报告：

```text
## 开发完成

### 本次完成

- [功能]

### 主要修改

- `文件路径`
- `函数/模块`

### 测试结果

- TypeCheck: PASS / FAIL / NOT RUN / BLOCKED
- Lint: PASS / FAIL / NOT RUN / BLOCKED
- Unit Test: PASS / FAIL / NOT RUN / BLOCKED
- Build: PASS / FAIL / NOT RUN / BLOCKED
- 用户流程: PASS / FAIL / NOT RUN / BLOCKED
- 回归测试: PASS / FAIL / NOT RUN / BLOCKED

### DevLog 更新

- `docs/devlog/...`

### 测试记录

- `docs/testing/...`

### 重要技术决策

- [如有]

### 遗留问题

- [问题]

### 阻塞事项

- [如有]

### 下一步

- [建议]
```

如果本次修改属于极小的局部调整，没有产生长期知识：

> `DevLog：本次修改属于局部实现，未产生新的长期架构、技术决策或维护知识，因此无需新增长期日志。`

---

# 30. 最终原则

若星空间的 Agent 开发遵循：

> **不盲改、不猜测、不假完成、不造数据、不跳测试、不丢上下文。**

始终坚持：

> **先理解，再行动；**
>
> **以产品为依据，以代码为事实；**
>
> **以测试证明完成，以 DevLog 保存记忆；**
>
> **以最小变更控制风险，以持续回归保证稳定。**

最终目标不是：

> “让 Agent 写出更多代码。”

而是：

> **让 Agent 持续把若星空间建设成一个真实可运行、可验证、可维护、可演进的产品。**
