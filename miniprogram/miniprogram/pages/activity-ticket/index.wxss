/* pages/activity-ticket/index.wxss */
.ticket-page {
  padding-top: 40rpx;
  padding-bottom: 80rpx;
}

.ticket-card {
  background: var(--starry-paper);
  border-radius: var(--starry-radius-lg);
  box-shadow: 0 10rpx 40rpx rgba(22, 48, 15, 0.08);
  overflow: hidden;
  border: 1rpx solid var(--starry-rice);
  margin-bottom: 40rpx;
}

.ticket-header {
  background: var(--starry-green);
  padding: 32rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.ticket-brand {
  font-size: 26rpx;
  font-weight: 600;
  color: #FFFFFF;
}

.ticket-status-badge {
  font-size: 20rpx;
  padding: 6rpx 16rpx;
  border-radius: var(--starry-radius-full);
  font-weight: 500;
}

.badge-pending {
  background: rgba(255, 255, 255, 0.2);
  color: #FFFFFF;
  border: 1rpx solid rgba(255, 255, 255, 0.4);
}

.badge-checked {
  background: var(--starry-bud);
  color: var(--starry-green);
}

.ticket-main {
  padding: 36rpx 32rpx;
}

.ticket-title {
  font-size: 34rpx;
  font-weight: 600;
  color: var(--starry-green);
  line-height: 1.4;
  margin-bottom: 24rpx;
}

.ticket-detail-row {
  display: flex;
  font-size: 24rpx;
  margin-bottom: 12rpx;
  line-height: 1.5;
}

.row-label {
  color: var(--starry-ash);
  width: 120rpx;
  flex-shrink: 0;
}

.row-val {
  color: var(--starry-ink);
  font-weight: 500;
}

/* 虚线与凹槽 */
.ticket-divider {
  position: relative;
  height: 40rpx;
  display: flex;
  align-items: center;
}

.punch-hole {
  position: absolute;
  width: 40rpx;
  height: 40rpx;
  border-radius: 50%;
  background: var(--starry-bg);
  border: 1rpx solid var(--starry-rice);
}

.left-hole {
  left: -20rpx;
}

.right-hole {
  right: -20rpx;
}

.dash-line {
  width: 100%;
  border-bottom: 2rpx dashed var(--starry-rice);
  margin: 0 32rpx;
}

/* 二维码区域 */
.ticket-qr-section {
  padding: 32rpx;
  text-align: center;
  background: var(--starry-paper);
}

.qr-box {
  width: 260rpx;
  height: 260rpx;
  margin: 0 auto 20rpx;
  background: #FFFFFF;
  border: 1rpx solid var(--starry-rice);
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 16rpx;
  box-sizing: border-box;
}

.qr-placeholder {
  display: flex;
  flex-direction: column;
  align-items: center;
  color: var(--starry-green);
}

.qr-mock-icon {
  font-size: 100rpx;
  line-height: 1;
}

.qr-mock-text {
  font-size: 18rpx;
  color: var(--starry-ash);
  margin-top: 10rpx;
}

.ticket-code-text {
  font-size: 28rpx;
  font-weight: 700;
  font-family: monospace;
  color: var(--starry-ink);
  letter-spacing: 2rpx;
  margin-bottom: 8rpx;
}

.ticket-hint {
  font-size: 20rpx;
  color: var(--starry-ash);
}

.actions-box {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.save-btn {
  width: 100%;
  padding: 24rpx 0;
  font-size: 28rpx;
}

.home-btn {
  width: 100%;
  padding: 22rpx 0;
  background: var(--starry-rice);
  color: var(--starry-tea);
  font-size: 28rpx;
  font-weight: 500;
  border-radius: var(--starry-radius-full);
}
